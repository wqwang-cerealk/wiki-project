name: Java lint

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  java-lint:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2

      - name: helloworld          
        run: echo Hello World
